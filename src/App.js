import React from "react";
import { Container, Button } from "./App.elements";

const App = () => {
  return (
    <Container>
      <Button>Show Popup</Button>
    </Container>
  );
};

export default App;
